//=================================================================
//Used to determine which layer an object belongs to
//=================================================================

#pragma once

enum ObjectLayer
{
	OL_NULL = 0,
	OL_CAMERA = 1,
	OL_LIGHT = 2
};